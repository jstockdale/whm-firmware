whm-bringup 0.55.0-phase0
built: 2026-09-09 (r144: FULL DEPTH - the panel fidelity release, because the panel is the whole point. The dim-color flicker's root was never load or compositing but BCM arithmetic: lsbMsbTransitionBit=1 shows the two LSB bit-planes ONCE per frame with shortened OE - a duty error on levels 1-3 plus a 150Hz flicker component exactly where dark under-rows live. The fix is correctness, not brute speed: output clock 20 -> 32 MHz (PLL/5; FM6126A typ tops 30, 32 is the proven short-leads setting) is the ONE clock that lets the driver's auto-picker reach transitionBit=0 at the 120Hz target - 150Hz@20M,T1 -> 240@32M,T1 -> ~120Hz@T0, every bit plane correctly binary-weighted, the once-shown-LSB class extinct at the root. And the sleeper: gamma LINEAR -> CIE1931 (the lib's own recommended LUT) - perceptual LED luminance, midtones and gradients gain real depth; the sunset sky is the showcase. BOOT-VERIFY on flash (this release's true gate runs on YOUR bench): expect 'lsbMsbTransitionBit=0', a refresh line near 120Hz, and 'CIE1931 LUT' in the HUB75 init block. If a panel objects to 32MHz (shimmer/ghost columns), the one-line fallback is HZ_27M in display_hal.cpp - but that keeps T=1, so report before retreating. Dim platform rows should now sit rock-solid in the same afternoon light that exposed them.)
idf: v5.5.2
board: Waveshare ESP32-S3-RGB-Matrix | panel: ADA P2.5 64x64 1/32 HUB75E
