whm-bringup 0.55.2-phase0
built: 2026-09-09 (r146: THE TRAPDOOR - the crash symbolized, named, and shut. The owner guessed "still not using our NVS thread for the key exchange?" and addr2line proved him right one layer deeper than either of us expected: crypto DID use the broker's front door - and whm_settings_set_str walked straight through it. settings.c:258 read 'if (use_direct() || strlen(val) >= VAL_MAX) d_set_str(...)': identity hex strings are 64 chars (pk) and 128 chars (sk), over VAL_MAX, so the RPC client SILENTLY fell through to the direct path - a flash write from the sync rx task at the first IDENT (~15s), the cache-guard assert, both units, both logs, exactly on schedule. Boot-path minting survived because init runs on an internal stack; only the rx-era pin writes died, which is why the fleet chimed, synced, and THEN rebooted. Fix: VAL_MAX 144 (request key widened 16->24 alongside), and the oversize fallthrough now SCREAMS instead of silently going direct - the next undersized string will print its name, not assert. Everything 0.55.1 won is intact and confirmed by these very logs: 26.67MHz, transitionBit=0 at 203Hz, CIE1931 7-bit, chime audible, 81KB internal free. This flash: idents land, 'identity: PINNED <peer>' prints, SEAL wraps flow, and the uptime counter finally gets to grow old. Then 'ble enable' on both to clear the (correctly) tripped whlink fuses.)
idf: v5.5.2
board: Waveshare ESP32-S3-RGB-Matrix | panel: ADA P2.5 64x64 1/32 HUB75E
