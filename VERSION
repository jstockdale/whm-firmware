whm-bringup 0.58.2-phase0
built: 2026-09-09 (r155: THE PURE STREAM & THE COMMON CLOCK - the owner diagnosed it himself: "we must have some more hidden state," and the hidden state was hiding inside a purity comment. wk_rnd claimed PURE f(anchor, step, draw#) - then computed with ++s_wk_draws, a mutating draw counter that made every value depend on HOW MANY decisions came before. Purity held only if every replica consumed identical draws per step; any conditional draw - the fresh-branches, the probe loops, the sandbox re-sims - shifted one unit's stream forever after. And it fed back: diverged streams meant different turns and sit-times ("appearing at the wrong times"), which meant SNAPs (dx to -105, 1.6 panels) and ownership thrash, which changed the conditionals, which shifted the streams further. Terrain stayed perfect because terrain never rolls dice. THE FIX, structural: wk_rnd(key) is stateless - pure hash of (anchor, step, per-site key); loop sites keyed by their natural shared identity (the platform mark key). The value depends on WHAT is being decided, never on how many decisions came before; conditional consumption is harmless by construction. Counter, per-step reset, and sandbox save/restore deleted whole. AND THE OWNER'S INSTRUMENT: every walker/fleet/SEAL/identity/[A]/[modeA] line opens with a UTC wall-clock stamp [HH:MM:SS.mmm] (SNTP-disciplined) - two captures finally align to the millisecond. The stamp itself flushed FOUR un-braced single-statement guards out of hiding across ui.c and sync.c (one of them silently making a failure-print unconditional) - all braced from the view, -Werror earning its keep. Verdict: one crown at pattern-enter, pixel-perfect seam transits both directions, SNAPs rare and sub-pixel, storms extinct - and any future divergence convicts itself in one paste.)
idf: v5.5.2
board: Waveshare ESP32-S3-RGB-Matrix | panel: ADA P2.5 64x64 1/32 HUB75E
