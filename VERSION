whm-bringup 0.62.0-phase0
built: 2026-09-09 (r171: THE ORACLE - the state-hash ladder, greenlit by the owner off the library roadmap and built the same night. O(1) detection of ANY sim divergence at exact-step granularity: it audits the fields nobody promises, catching the last injector class BY CONSTRUCTION. The hash walks a CANONICAL FIELD MANIFEST, never raw struct bytes (padding is nondeterminism bait): six pose floats, eight packed bytes (st/dir/sdir/phase/turn_cd/fresh/timer), and the One-Camera double as the STRICT core; the seen/visits ring folds into a SOFT 16-bit tag reported only inside confessions, so the documented-unsynced fields never make the oracle cry wolf. Rungs every 32 steps (~1.07s, 8 bytes/s of wire): the fleet conductor publishes as the reference model (type 16, sizeof-gated FROM BIRTH, sealed); followers bank foreign rungs four deep and judge against their own eight-rung cache at EQUAL step, epoch-keyed by anchor byte, silent during replay per the Quiet law. Match: or= ticks on the [W] line (expect ~+1/s on the follower). Mismatch: a UTC-stamped ORACLE DIVERGENCE confession - step, both core hashes, both soft tags, and the full current manifest - so a serial diff names the guilty FIELD offline; soft-only divergence is the visits-class fingerprint. PLUS THE HARNESS HALF: tools/whm_oracle_diff.py ships in the source tarball - feed it two captures and it aligns ledgers by step, reports the first sx/cam/st divergence with both UTC stamps, and lists every oracle confession: the exact two-log forensics performed by hand through the 0.60.x hunts, now one command. Verdict card: or= climbing beside ok= on the non-conductor; any ORACLE line = the final injector caught red-handed with its field manifest attached - bring the pair of logs and the harness output, and we indict by breakfast.)
idf: v5.5.2
board: Waveshare ESP32-S3-RGB-Matrix | panel: ADA P2.5 64x64 1/32 HUB75E
