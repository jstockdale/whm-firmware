whm-bringup 0.58.3-phase0
built: 2026-09-09 (r156: THE WHOLE POSE - the owner ordered a full hidden-state audit after the sims diverged again IMMEDIATELY, and the audit convicted the corrector itself: the keyframe was a PARTIAL POSE. s_wk carries twelve fields; the wire and ring carried eight - missing vy, phase, sdir, turn_cd, spd, and fresh. Every adopt and every SNAP restored the owner's position while leaving the follower's OWN stroll speed (0.75 vs 0.55 = 12 px/s, the exact observed growth rate), its own fresh flag (gating different decision branches), its own mid-jump vy. The correction mechanism was re-injecting divergence at the moment of correction - which is precisely what "immediately started diverging again" looks like from the glass. The Pure Stream (0.58.2) was necessary and stands; it removed the stream-shift class. This removes the reinjection class: all six fields now ride the type-9 wire (whm_wkb_t, +12 bytes, assert updated - co-flash both units), the keyframe ring, whm_ui_wkb_rx, and the SNAP corrector - which gains triggers on spd/fresh/turn_cd/sdir mismatches and |dvy|>0.2, so partial-pose drift can never again hide below the dx threshold; a firing SNAP now names WHICH field diverged. Audit record in full: wk_visits reward memory unsynced (soft residual, self-heals each 10-min anchor window; documented, not over-engineered); s_wk_pure bisection flag volatile-only; the sim closure reads ZERO unit-local state (no s_w_idx, no wk_i_own, no s_wko) - idx-blind as doctrine demands; type-11 inputs apply at stamped exec_step. Verdict: one adopt at pattern-enter, then SNAP silence - timestamped, field-attributed if ever broken.)
idf: v5.5.2
board: Waveshare ESP32-S3-RGB-Matrix | panel: ADA P2.5 64x64 1/32 HUB75E
