whm-bringup 0.55.3-phase0
built: 2026-09-09 (r147: THE HIGH GROUND - the owner's wish, granted structurally. He wrote: "I wish we could shim all of the non-system calls so we didn't have to chase them down one by one" - and this capture proved him right in the strongest possible way: the symbolized trace is a READ. recv_task -> whm_pin_get -> whm_settings_get_str -> d_get_str -> nvs_page:895: the GET side has its own trapdoor (out_len > VAL_MAX goes direct), and NVS READS can erase flash (lazy page compaction, walking the dirty pages the crash spree itself littered). You cannot shim a library's own internals - so instead of shimming N call sites, THE CALLER MOVES. recv_task ran on a PSRAM stack from the 24KB-internal era; the depth-7 dividend returned 81KB, and it now runs on 6KB of plain internal RAM. Every flash touch from that task - writes, reads, and nvs's own housekeeping - is safe BY CONSTRUCTION, forever. The broker remains as hygiene (ld() now passes <=VAL_MAX so gets ride it too), but correctness no longer depends on winning whack-a-mole. The lineage converged exactly as layers peeled: mint (boot path) -> pin write (VAL_MAX) -> pin read (library compaction); each capture named the next; this one ends the family. Expected serial: PINNED lines, SEAL flows, uptime that grows old. Then 'ble enable' both, 'status', and the premiere.)
idf: v5.5.2
board: Waveshare ESP32-S3-RGB-Matrix | panel: ADA P2.5 64x64 1/32 HUB75E
