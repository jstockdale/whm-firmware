whm-bringup 0.50.0-phase0
built: 2026-09-08 (r131: FLEET OTA GROWS JUDGMENT. Bench autopsy with a twist: the self-guard already existed - but 'fleet ota @One.local' queues the literal '@One.local' fleet-wide, the fleet-addressing sugar leaked into every argv, the guard compared '@One' vs 'One' and MISSED, and HTTP got a garbage hostname: the 'cannot connect to itself' was the @ all along. Fixed three-layered per the owner's design: '@' is normalized once (guard AND url); a VERSION-AWARE SKIP reads the source's fw straight from announce gossip - if the source runs what I run there is nothing to pull, zero new wire; and 'ota <host> force' bypasses the version check only - the source-guard is absolute, keyed on SOURCE not anchor-role, so updating the anchor FROM another node stays legal forever. Correct flow: USB-flash the source, then one 'fleet ota <source>' updates exactly the stale nodes and nothing else.)
idf: v5.5.2
board: Waveshare ESP32-S3-RGB-Matrix | panel: ADA P2.5 64x64 1/32 HUB75E
