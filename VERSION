whm-bringup 0.45.0-phase0
built: 2026-09-08 (r117: THE BOOT LOOP KILLED - bench conviction from the first hardware flash of the wh-link arc: BLE_INIT Malloc failed -> controller assert emi.c 164 -> IWDT panic -> loop. The controller needs ~60-70KB internal and does NOT return on failure - it asserts; our runtime pool was 31KB (largest block 18KB). Three prongs: DIET (NimBLE host allocs -> PSRAM, 1 connection, peripheral-only roles, MAX_ACT 3, scan-dup cache off); ORDERING (whlink init moved post-display pre-wifi - the controller draws from a clean ~118KB pool; a panel must show something, so display keeps first); ARMOR (pre-flight skips BLE loudly if largest internal block <50KB, plus a 3-strike boot fuse that self-disables BLE with instructions, auto-clearing after 30s healthy; ble enable clears manually). Includes 0.44.2 chair/fireworks fix. Flash this to end the loop.)
idf: v5.5.2
board: Waveshare ESP32-S3-RGB-Matrix | panel: ADA P2.5 64x64 1/32 HUB75E
