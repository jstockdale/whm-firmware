whm-bringup 0.57.0-phase0
built: 2026-09-09 (r150: FIRST LIGHT - P1 lands whole, and the owner's streaming ask turns it into a flagship. THE CONTAINER: .whm (32B header + per-frame RGB888 row-RLE, every frame a seek point; docs/MEDIA.md is the spec); tools/whmconv.py converts anything Pillow or ffmpeg can open. THE PLAYER: pattern 'media', frames as pure f(TSF) - the same physics that locked the walker and the music, pointed at pixels. 'media play f.whm' boundary-starts on the 500ms TSF grid, so 'fleet media play' lands every node in the SAME slot with zero extra wire; fleet-wide files (w>64) crop per node at strip*64 - one 128x64 movie SPANS THE BEZEL because the format never knew the bezel existed. THE LIVE LANE: POST /stream/frame on the anchor ingests RLE frames (same format - one decoder serves both lanes), displays its crop, and fans type-15 fragments (<=1100B) to the fleet; every node maps pts->tsf off the first frame (+150ms budget) and schedules identically - LIVE video hits both panels at the same TSF instant; loss drops a frame and holds the last. THE BROWSER CLIENT ships INSIDE the firmware: open http://one.local/live, tap Share screen (or Camera, or drop a video file), and the fleet mirrors your desktop through the bezel - zero install, the encoder is 15 lines of JS. tools/whmcast.py is the CLI twin (mss screen grab or ffmpeg file). Try tonight: python3 tools/whmconv.py sunset.gif sunset.whm --size 128x64 --loop; copy to SD (or sync media); 'fleet media play sunset.whm' - then open /live and share your screen at the panels.)
idf: v5.5.2
board: Waveshare ESP32-S3-RGB-Matrix | panel: ADA P2.5 64x64 1/32 HUB75E
